#include "analyzer.hpp"

namespace phi {
    using namespace ast;
    void Analyzer::analyze(Ref<Node> node) {
        switch (node->type()) {
        case Node::Type::NODE:
            break;
        }
    }
} // namespace phi